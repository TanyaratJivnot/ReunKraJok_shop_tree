import { ProductPayDirective } from './product-pay.directive';

describe('ProductPayDirective', () => {
  it('should create an instance', () => {
    const directive = new ProductPayDirective();
    expect(directive).toBeTruthy();
  });
});
