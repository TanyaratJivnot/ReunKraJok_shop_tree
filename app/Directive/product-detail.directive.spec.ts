import { ProductDetailDirective } from './product-detail.directive';

describe('ProductDetailDirective', () => {
  it('should create an instance', () => {
    const directive = new ProductDetailDirective();
    expect(directive).toBeTruthy();
  });
});
